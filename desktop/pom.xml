<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
           http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ar.com.rosario.realestate</groupId>
    <artifactId>rosario-realestate</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>desktop</artifactId>
  <name>desktop</name>
  <description>JavaFX 21 desktop application — AtlantaFX theme, OAuth2 PKCE, local-first</description>

  <dependencies>
    <dependency>
      <groupId>ar.com.rosario.realestate</groupId>
      <artifactId>persistence</artifactId>
    </dependency>
    <dependency>
      <groupId>ar.com.rosario.realestate</groupId>
      <artifactId>auth</artifactId>
    </dependency>
    <dependency>
      <groupId>ar.com.rosario.realestate</groupId>
      <artifactId>ml</artifactId>
    </dependency>
    <dependency>
      <groupId>ar.com.rosario.realestate</groupId>
      <artifactId>scraper</artifactId>
    </dependency>

    <!-- JavaFX 21 — OS classifier resolved by os-maven-plugin extension -->
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
      <classifier>${os.detected.classifier}</classifier>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>${javafx.version}</version>
      <classifier>${os.detected.classifier}</classifier>
    </dependency>
    <dependency>
      <groupId>io.github.mkpaz</groupId>
      <artifactId>atlantafx-base</artifactId>
    </dependency>
    <dependency>
      <groupId>org.controlsfx</groupId>
      <artifactId>controlsfx</artifactId>
    </dependency>
    <dependency>
      <groupId>org.kordamp.ikonli</groupId>
      <artifactId>ikonli-javafx</artifactId>
    </dependency>
    <dependency>
      <groupId>org.kordamp.ikonli</groupId>
      <artifactId>ikonli-materialdesign2-pack</artifactId>
    </dependency>

    <!-- Spring Boot for local datasource / Flyway -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <!-- Caching -->
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <extensions>
      <extension>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>1.7.1</version>
      </extension>
    </extensions>

    <plugins>
      <!-- jpackage native installer (Phase 20) -->
      <plugin>
        <groupId>org.panteleyev</groupId>
        <artifactId>jpackage-maven-plugin</artifactId>
        <version>1.6.5</version>
        <configuration>
          <name>RosarioRealEstate</name>
          <appVersion>${project.version}</appVersion>
          <mainClass>ar.com.rosario.realestate.desktop.DesktopLauncher</mainClass>
          <mainJar>${project.artifactId}-${project.version}.jar</mainJar>
          <destination>${project.build.directory}/dist</destination>
          <javaOptions>
            <option>--add-opens=javafx.graphics/com.sun.javafx.application=ALL-UNNAMED</option>
          </javaOptions>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
